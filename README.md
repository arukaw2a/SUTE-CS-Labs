## Task 1

A Python program that accepts two three-digit numbers and calculates the sum and product of their last digits.

User input is handled using the `input()` function.

## Task 2

A Python program that accepts the number of sides of a regular polygon and calculates the measure of one of its interior angles.

The input value is passed to the program through command-line arguments using `sys.argv`.
